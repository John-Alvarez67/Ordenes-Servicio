services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
  db:
    image: postgres
    environment:
      POSTGRES_USER: juan
      POSTGRES_PASSWORD: 12345678
      POSTGRES_DB: web
